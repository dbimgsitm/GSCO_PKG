# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: com.worldex.co.zworldexco0010
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://my405699.s4hana.cloud.sap/
          authenticationType: reentranceTicket # SAML support for vscode
        app:
          name: YY1_GLMTMASTER
          description: 실제원가-이동평균 수불부 기준정보
          package: TEST_YY1_DEFAULT
          transport: ''
        exclude:
          - /test/
